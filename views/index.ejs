<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trend Analysis</title>
    <link rel="stylesheet" href="/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
</head>
<body>
    <%- include('partials/header') %> 

    <div class="container mt-5">
        <div class="welcome-section text-center">
            <h1 class="display-4 text-primary mb-3">Welcome to Trend Analysis</h1>
            <p class="lead text-muted">Discover and analyze trending topics in real-time.</p>
        </div>

        <% if (error) { %>
            <div class="alert alert-danger mt-4" role="alert">
                <%= error %>
            </div>
        <% } %>

        <div class="form-section mt-5">
            <form action="/getTrends" method="post" class="p-4 shadow rounded bg-white">
                <div class="mb-4">
                    <label for="queries" class="form-label fw-semibold">Enter your queries (comma-separated):</label>
                    <input type="text" id="queries" name="queries" class="form-control" placeholder="E.g., Pushpa, Stree, Devara" required>
                </div>
                <button type="submit" class="btn btn-primary w-100 py-2">Analyze Trends</button>
            </form>
        </div>

        <div class="instructions mt-5 text-muted text-center">
            <h3 class="mb-3">How to Use</h3>
            <p>1. Enter keywords or topics separated by commas.<br> 
               2. Click "Analyze Trends" to see visual data and insights.<br> 
               3. Download results for future use.</p>
        </div>
    </div>

   
</body>
</html>
